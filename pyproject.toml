[project]
dynamic = ["classifiers", "keywords", "license", "authors", "scripts"] = "camie_distill"
version = "0.1.0"
description = "Student‑teacher distillation toolkit for Camie‑Tagger"

# One directory up from Deepone/
readme = { file = "../README.md", content-type = "text/markdown" }

requires-python = ">=3.10"
dependencies = [
    "torch>=2.1",
    "timm==0.9.12",
    "safetensors>=0.5.3",
    "deepspeed>=0.16.3,<0.17",
]